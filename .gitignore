# Local credentials — copy from config.template.h and fill in
main/config.h

# IDF build artifacts
build/
sdkconfig
sdkconfig.old

# IDF managed components — fetched from registry per dependencies.lock
managed_components/

# Local debug screenshots
IMG_*.jpg
